# program to return/print the string that has max character in list
list1 = ["python", "programming", "abc"]
print("The given string list is:",list1)
charcount = []
num1 = 1
for i in list1:
    count = 0
    for j in i:
        count += 1
    charcount.append(count)
# print(charcount)
largest = charcount[0]
for i in charcount:
    if i > largest:
        largest = i
# print(largest)
# print(charcount.index(largest))
largstr = charcount.index(largest)
print("The string that has max character in it :",list1[largstr])



OR

list1 = ["python", "java12345", "abcdefghijk"]

countl = []
for i in list1:
   countl.append(len(i))

max_char_count = countl[0]
for j in countl:
    if j > max_char_count:
        max_char_count = j
            
print(list1[countl.index(max_char_count)])
